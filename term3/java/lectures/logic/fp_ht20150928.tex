\begin{enumerate}
	\item
		\begin{itemize}
			\item
				$$S\ K\ K = (\lambda\ f^{\alpha \rightarrow (\beta \rightarrow \alpha) \rightarrow \alpha}
						   \ g^{\alpha \rightarrow \beta \rightarrow \alpha}\ x^{\alpha}.
						   \ f\ x(g\ x))(\lambda\ x\ y.\ x)(\lambda\ x\ y.\ x)
				$$, 
				$$(\alpha \rightarrow (\beta \rightarrow \alpha) \rightarrow \alpha) \rightarrow 
		  		(\alpha \rightarrow \beta \rightarrow \alpha) \rightarrow \alpha \rightarrow \alpha$$
			\item
				$$
				S\ K\ I = (\lambda\ f^{\alpha \rightarrow \alpha \rightarrow \alpha} 
						    g^{\alpha \rightarrow \alpha}
						    x^{\alpha}. f\ x(g\ x))(\lambda\ x^{\alpha} y^{\alpha}. x)(\lambda\ x^{\alpha}. x)
				$$,
				$$
				(\alpha \rightarrow \alpha \rightarrow \alpha) \rightarrow (\alpha \rightarrow \alpha) 
				\rightarrow \alpha \rightarrow \alpha
				$$	
		\end{itemize}
	\item
		\begin{itemize}
			\item
				$$\lambda\ x\ y\ z.\ x$$
				Могу привести \i{один} терм.
			\item
				$$\lambda\ x\ y\ z.\ y$$
				Иогу привести \i{один} терм.
			\item
				$$\lambda\ x\ y\ z.\ x$$
				$$\lambda\ x\ y\ z.\ z$$
				Могу привести \i{два} терма.
			\item
				$$\lambda\ x\ y\ z.\ x$$
				$$\lambda\ x\ y\ z.\ y$$
				$$\lambda\ x\ y\ z.\ z$$
				Могу привести \i{три} терма.
		\end{itemize}
	\item
		\begin{itemize}
			\item
				$$\lambda\ f^{\delta\rightarrow\delta\rightarrow\alpha}
					 \ g^{\alpha\rightarrow\beta\rightarrow\gamma}
					 \ h^{\delta\rightarrow\beta}\ x^{\delta}. g(f\ x\ x)(h\ x)$$
			\item
				$$\lambda\ f^{\delta\rightarrow\delta\rightarrow\alpha}
					 \ g^{\gamma\rightarrow\alpha}
					 \ h^{\alpha\rightarrow\beta}
					 \ x^{\delta}.\ h\ g$$
				$$\lambda\ f^{\delta\rightarrow\delta\rightarrow\alpha}
					 \ g^{\gamma\rightarrow\alpha}
					 \ h^{\alpha\rightarrow\beta}
					 \ x^{\delta}
					 \ y^{\gamma}.\ h(g\ y)$$
				$$\lambda\ f^{\delta\rightarrow\delta\rightarrow\alpha}
					 \ g^{\gamma\rightarrow\alpha}
					 \ h^{\alpha\rightarrow\beta}
					 \ x^{\delta}
					 \ y^{\gamma}.\ h(f\ x\ x)$$
		\end{itemize}
	\item
		$$\lambda\ x^{\gamma\rightarrow\epsilon}
			 \ y^{(\gamma\rightarrow\epsilon)\rightarrow\epsilon}.\ y(\lambda\ z^{\gamma}.\ x\ z)$$
		При этом внутренний терм имеет тип $\gamma\rightarrow\epsilon$, что даёт нам $\epsilon$ в результате применения $y$.
	\item
		\begin{itemize}
			\item
				$$\lambda\ f^{(\alpha\rightarrow\beta)\rightarrow\alpha}
			 		 \ g^{\alpha\rightarrow\alpha\rightarrow\beta}.\ f(\lambda\ x^{\alpha}.\ g\ x\ x)$$
				Внутренний тип имеет тип $\alpha\rightarrow\beta$, что даёт нам $\alpha$ в результате применения $f$.
			\item
				Обозначим $f(\lambda\ x^{\alpha}.\ g\ x\ x)$ за $A$
				(тип результата будет $\alpha$ при нашем типе $f$ и $g$):
				$$\lambda\ f^{(\alpha\rightarrow\beta)\rightarrow\alpha}
			 		 \ g^{\alpha\rightarrow\alpha\rightarrow\beta}.g\ A\ A$$
				Тогда итоговое выражение имеет искомый тип.
		\end{itemize}
\end{enumerate}
